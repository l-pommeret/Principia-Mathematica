# Gate kernel ✱4·87

Locally compiled candidate. `equivChain4` is the audited diplomatic abbreviation `((A≡B)·(B≡C))·(C≡D)`, not nested equivalence. Its three components are explicit exportation, commuted curryfication, and importation; their packaging uses only explicit primitive inference branches.
