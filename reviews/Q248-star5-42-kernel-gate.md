# Gate kernel ✱5·42

Candidate awaiting CI. Both directions are constructed under explicit `p` and `q`: ✱3·31 turns each curried conditional into a conjunction antecedent, ✱3·35 supplies its conclusion, and the forward branch pairs `p` with `r` while the reverse branch projects `r`. The result is curried twice and reassembled with ✱3·2; all inference branches are explicit and no generic detachment or oracle is used.
