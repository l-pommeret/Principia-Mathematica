# Gate kernel ✱5·42

Kernel-checked by CI run `31554037055` at commit `bc34b63d279a21b730eb17beffe8d4512f037921`. Both directions are constructed under explicit `p` and `q`: ✱3·31 turns each curried conditional into a conjunction antecedent, ✱3·35 supplies its conclusion, and the forward branch pairs `p` with `r` while the reverse branch projects `r`. The result is curried twice and reassembled with ✱3·2; all inference branches are explicit and no generic detachment or oracle is used.
