# ✱121 catalogue 03 strict semantic audit

All five loci PM2:✱121·102, ·103, ·11, ·12, and ·121 pass strict typed
equivalence. The first two are exactly the pointwise membership laws for the
right-closed and closed interval definitions. The other three unfold,
respectively, a distance level, the class of finite-distance levels, and its
positive-index subclass. `Nat` represents the admissible non-null inductive
cardinals and `n > 0` preserves the further exclusion of zero.

The five source readings match their literal blocks. None prints a numbered
dependency, and each Lean proof is a judgmental unfolding with no theorem
dependency. All five records are promoted to `awaiting-ci`; no item is refused
and CI evidence remains pending.
