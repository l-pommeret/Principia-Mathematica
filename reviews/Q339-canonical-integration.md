# Q339 canonical integration

This batch uses the same explicit simple-type embedding already audited at
✱21·151–17: a binary relation is `RelationExtension α β`, and a class
argument at ✱21·704 is the independently typed `ClassExtension γ`. In this
embedding every well-typed function is already an admissible predicative
representative, so each existential is witnessed by the function itself.
✱21·702 alone reorders its arguments to match the printed `g!(R,x)`.

The implementation preserves every displayed argument order and never
collapses relation, object, or class types. It introduces no reducibility
axiom, choice, untyped relation universe, unsafe declaration, or generic
assertion rule. The printed “Proof as in ✱20·…” citations describe the
historical ramified route; the metadata records their compression by the
simple-type embedding explicitly.
