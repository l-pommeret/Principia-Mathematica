# Q368 canonical integration

The five propositions are proved pointwise over the typed class carrier from
✱22·01–05. ✱24·41 splits membership by classical excluded middle on `x ∈ β`;
✱24·411 replaces the intersection part using `β ⊂ α`; ✱24·412 performs the
same split across `γ ⊂ β ⊂ α`. ✱24·42 packages the two cases as an inclusion
equivalence, and ✱24·43 is the corresponding difference/union adjunction.

Each resulting pointwise equivalence is converted to class identity by
function and proposition extensionality. The source record supplies no
explicit citations at these five loci, so the bodies use no earlier PM
theorem calls. They introduce no untyped universal class, choice,
reducibility axiom, unsafe declaration, or generic assertion rule. Classical
case distinction is inherited from PM's elementary logic.
