# ✱53 seventh semantic audit

Verdict: **awaiting-ci** for ✱53·35, ·4, and ·5; no refusal.

- **✱53·35** preserves both equalities of the printed chain. The first
  conjunct computes the image of the two singleton families under class sum;
  the second is the extensional distribution of class sum over union.
- **✱53·4** makes contextual definedness explicit as
  `h : FunctionalAt r x`. Its chosen value, unit forward image, membership in
  that image, singleton equality, and unique-member recovery are precisely the
  four printed descriptions. The explicit witness does not assume any one of
  the equivalences being proved.
- **✱53·5** interprets contextual existence of the class `a` as existence
  of a member and `a ∈ Cls − ιʻΛ` as `a ≠ empty`; this is the exact typed
  nonempty-class criterion.

The implementation edges from ·35 to ·34/·11 and from ·4 to ·31/·3 are
recorded as relaxed closure because the printed statements carry no bracketed
citations. The proof of ·5 is direct.
