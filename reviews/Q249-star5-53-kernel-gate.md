# Gate kernel ✱5·53

Candidate awaiting CI. The canonical body instantiates ✱4·77 at `p, q` and at `p ∨ q, r`; ✱4·36 transports the first equivalence under the common factor `r ⊃ s`. It extracts both directions with ✱3·26/27, composes them using ✱2·05, and packages the displayed equivalence with ✱3·2. All detachment branches are explicit over empty and non-empty real-variable contexts; no generic detach rule or oracle is used.
