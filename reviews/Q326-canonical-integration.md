# Q326 canonical integration

The batch stays at one explicit simple type. `ClassExtension α` is the typed
membership predicate already established for ✱20·19; no untyped universal
class is introduced. ✱20·71 is exactly that audited Leibniz theorem.

For ✱20·8, both printed class abstractions are represented extensionally:
`x ↦ φ x ∨ ¬φ x` and `x ↦ x = a ∨ x ≠ a`. PM's classical elementary logic
makes both universally true, so function extensionality closes their class
identity. ✱20·81 composes two instances of ✱20·8 through the common identity-
decision extension. The printed hypotheses are retained verbatim even though
they are redundant in the classical logic. No reducibility axiom, choice,
class-valued term, unsafe declaration, or new assertion rule is introduced.
Direct normalization compresses unused historical citations, which the
metadata records explicitly.
