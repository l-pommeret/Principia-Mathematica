# ✱95 catalogues 03–04 strict semantic audit

The next ten loci, split in printed order into two batches of five, are
·212, ·22, ·221, ·222, ·23 and ·24, ·3, ·301, ·302, ·303. The
canonical readings on printed pages 628–629 (scan leaves 650–651) were checked
against Project Gutenberg 78050 and the first-edition facsimile. All ten IDs
are new and have exactly one PM-VERBATIM block and one metadata record.

No declaration passes strict equivalence, so both batches remain homogeneous
`prepared/refused` batches. Items ·212 and ·22 erase PM's domain/range premises,
power/Potid memberships and factorization equations. Items ·221 and ·222
restrict the supplied power to exponent zero and omit membership of the
resulting factor in the equi-factor class. Items ·23 and ·24 define
`SameOrbit` to be `Equi` itself, rather than representing either printed
similarity-transform product.

Items ·3 and ·301 replace PM's existence/uniqueness and Potid hypotheses by
new pointwise-totality assumptions and prove only nonemptiness. Items ·302 and
·303 replace Potid membership by unrelated direct inclusions; their Lean
bodies visibly leave two purported source hypotheses unused. These are
mathematical changes, not harmless dependency relaxations. Every refusal is
therefore explicit and no item is promoted. Printed proof-as-in edges are
retained in metadata; the parser route is `reviewed-gap` for historical
power, Potid, domain/range and similarity-transform notation.
