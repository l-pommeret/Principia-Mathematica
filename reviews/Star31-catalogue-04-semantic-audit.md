# ✱31 catalogue 04 strict semantic audit

Scope: exactly ✱31·33, ·34, ·4, ·41, and ·5, checked against the literal
PG78050 blocks and the second and third converse kernels.

All five declarations pass strict homogeneous typed equivalence.  ·33 is
converse involution and ·34 is its equality symmetry.  The predicate
`subrelation P Q := ∀ x y, P x y → Q x y` is the typed reading of the printed
relation inclusion in ·4 and ·41.  The predicate `relationExists` is the
existential closure of a relation, so ·5 retains exactly nonemptiness of `P`
and of its converse rather than descriptive value existence.

The proof of ·34 calls the already audited ·33 involution although the compact
printed line gives no bracketed citation; this added proof edge is explicit in
the reviewed relaxed closure.  The remaining proofs use only definitions.
Printed citations for ·4, ·41, and ·5 therefore remain historical unused
edges, including the named `Transp` move.  No dependency beyond the explicit
·34/·33 edge is introduced.  All five records are promoted to `awaiting-ci`,
with CI evidence pending.

