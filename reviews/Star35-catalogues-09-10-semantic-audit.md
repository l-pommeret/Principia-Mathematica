# ✱35 catalogues 09–10 strict semantic audit

Both five-item lots pass strict source-to-Lean equivalence; no refusal split is
required.

Catalogue 09 first records the immediate inclusion of a doubly restricted
relation in its underlying relation.  In ✱35·451–·454, the Lean domain and
converse-domain predicates have PM's source and target orientations
respectively.  Each related pair supplies exactly the witness needed to remove
the endpoint restriction under the printed inclusion hypothesis.

Catalogue 10 preserves relation-inclusion orientation in ·46, ·461, and ·462.
For ·471, the intermediate object in composition belongs to the converse-domain
of `P` and to the left restriction class, so the printed disjointness premise
forces emptiness.  For ·472, the same intermediate object belongs to the domain
of the second relation `P` and to the right restriction class.  Thus the two
apparently dual statements use `cod P` and `dom P` in exactly the correct
places; neither is swapped.

All ten records are promoted in place to `awaiting-ci`.  Existing parser-gap
metadata remains independent of the semantic verdict.
