# ✱93 catalogues 08–09 — strict source/Lean semantic audit

Scope: ✱93·32, ·33, ·34, ·35, ·36, ·37, ·38, ·381, ·382,
and ·4 (printed pages 613–614, scan leaves 635–636). All ten readings
agree with their unique canonical source blocks.

| PM locus | verdict | strict obstruction |
|---|---|---|
| ✱93·32 | refused | Opaque `Representation` replaces the printed existential Potid representation, and the whole equivalence is premise `h`. |
| ✱93·33 | refused | The closure of generated classes under converse image is exactly the supplied implication `h`. |
| ✱93·34 | refused | Generated membership of the converse image of the boundary is exactly premise `h`. |
| ✱93·35 | refused | The complete three-antecedent closure theorem is passed unchanged as `h`; local `Potid` is also degenerate. |
| ✱93·36 | refused | Arbitrary `S` replaces `sʻgenʻP`; `reflClosure` is only one-step identity union relation, and the equality is premise `h`. |
| ✱93·37 | refused | Arbitrary `S/H` replace both printed union components and the target field equality is premise `h`. |
| ✱93·38 | refused | Arbitrary `H` replaces the Pot-domain image, one-step `reflClosure` replaces the ancestral, and the equivalence is premise `h`. |
| ✱93·381 | refused | Same defects for the converse orientation: erased class, one-step closure, target supplied as `h`. |
| ✱93·382 | refused | Arbitrary `H/K`, one-step closures, and the complete bifunctional equivalence supplied as `h`. |
| ✱93·4 | refused | This repeats the printed field decomposition using arbitrary classes and returns hypothesis `h` unchanged. |

The strict result is **0 exact / 10 refused**. Catalogues 08 and 09 are
homogeneous five-item refused manifests, with unique IDs and pending CI
evidence. Their empty dependency graphs are consistent with pass-through
bodies and do not establish the omitted mathematics.
