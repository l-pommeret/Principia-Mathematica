# PM I ✱64 catalogue 04 strict semantic audit

All five items are refused and remain `prepared`. The Lean namesakes for ·13–·16
replace PM's heterogeneous relations, pair typing, and relation subclasses by
reflexive `Same`/`t` facts. For ·2, Lean assumes `Same x y` and concludes only
`t x = t y`; it omits both the printed intersection-existence premise and the
membership conjunct. These are substantive target changes.

All five unchanged strings fail the current parser, matching their existing
reviewed gaps. Dependency graphs stay empty because no declaration is accepted;
there is one record per ID and no split sidecar.
