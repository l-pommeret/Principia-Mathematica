# Gate kernel ✱5·61 — PM I, p. 130

Kernel-checked by CI run `31554631470` at commit `0a4db2de190c41b4a5f6308fc0b4511fb716747d`. The forward direction projects `p ∨ q` and `∼q`, commutes the disjunction, and applies ✱2·55; the reverse direction adds `q` by ✱2·2. Both retain `∼q` by explicit conjunction reconstruction. No generic detachment, Aristotle-local interface, oracle, axiom, placeholder, semantics, native connective, or source correction is used.
