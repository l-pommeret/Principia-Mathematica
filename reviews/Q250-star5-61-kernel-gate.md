# Gate kernel ✱5·61 — PM I, p. 130

Candidate awaiting CI. The forward direction projects `p ∨ q` and `∼q`, commutes the disjunction, and applies ✱2·55; the reverse direction adds `q` by ✱2·2. Both retain `∼q` by explicit conjunction reconstruction. No generic detachment, Aristotle-local interface, oracle, axiom, placeholder, semantics, native connective, or source correction is used.
