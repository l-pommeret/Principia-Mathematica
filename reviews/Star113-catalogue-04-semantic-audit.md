# PM II ✱113 catalogue-04 strict semantic audit

The five p. 110 namesakes ·121–·125 were audited by exact theorem type.  None
is promotable.  ·121 reduces PM's union-of-fibres similarity to self-similarity
of `Product`; ·122 assumes and returns the entire one-one conclusion; ·123 and
·124 are reflexive propositional surrogates; and ·125 is reflexive equality
of a single generic image.  The printed correlator, family, image, functionality
and range structures are consequently not preserved.

All five remain `prepared` in the refused-only catalogue.  Direct parser tests
reject their diplomatic operators (`↓`, `×`, restriction/correlator notation),
so each is a documented `reviewed-gap`; the printed readings are unchanged.
