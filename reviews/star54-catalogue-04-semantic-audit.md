# ✱54 catalogue-04 semantic audit

✱54·42 and ·43 are exact in the typed class model: they respectively remove
the empty subclass case and characterize disjoint singleton union.

✱54·44, ·441 and ·442 are refused. Their Lean declarations establish only
that two distinct members of a pair occur in one of two orders. The printed
claims quantify an arbitrary predicate `φ` and assert an equivalence or
implication about `φ`; that logical content is absent. In ·442 the additional
`x ≠ y` hypothesis is unused, independently confirming the mismatch.
