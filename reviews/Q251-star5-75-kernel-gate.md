# Gate kernel ✱5·75 — PM I, p. 131

The proof reconstructs the printed hypothesis `Hp` as
an explicit conjunction. It extracts both directions of the equivalence,
derives `p ∧ ∼q ⊃ r` through ✱5·6, derives `r ⊃ p ∧ ∼q` through the printed
projection and composition steps, then curries the resulting equivalence.
No oracle, axiom, placeholder, semantics, native connective, or generic
cross-context detachment is used.

Remote kernel certification: run `31559051178` succeeded at commit
`ed8417a626504193bfb7d5c31c306a1d2da16b2d`, including aggregate preflight,
all isolated elementary contexts, and the complete edition build.
