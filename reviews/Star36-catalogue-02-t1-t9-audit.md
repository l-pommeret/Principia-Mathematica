# PM I ✱36 catalogue 02 T1–T9 audit

All five records are secondary host propositions, not PM object judgments.
They therefore revert to `prepared` and are blocked. None has a concrete
relation-formula AST, an inductive derivation endpoint, or a valid reading
linking the diplomatic source to such an endpoint (T3–T4).

Kernel axiom inspection is also decisive: ·202, ·203, ·21, and ·23 depend on
`propext` and `Quot.sound`. Only ·22 is axiom-free, but it remains a direct
host-level witness proof rather than a derivation in the printed calculus.

The graphs were rebuilt from Gutenberg's demonstrations. Printed edges are
·202→·2; ·203→✱35·18; ·21→{✱35·33,✱35·34};
·22→{✱36·13,✱34·1,✱10·5,✱10·35}; and ·23→✱35·422.
The actual Lean graph is empty for every item: each body reasons directly with
host functions and propositions. Consequently every normalized graph is empty
and every printed edge is explicitly `printed_but_unused`; this records a
missing historical route, not an accepted relaxation.
