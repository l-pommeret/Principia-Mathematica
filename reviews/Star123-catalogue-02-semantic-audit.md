# ✱123 catalogue-02 semantic audit

This strict audit covers exactly the next five canonical loci, ✱123·12–·16,
on printed pages 269–270 (scan leaves 309–310). The diplomatic source blocks
were checked against PG78255 and the 1912 Volume II witness.

No item is promoted. ✱123·12 requires the source's initial-term class `B⃗ʻR`
and its unit-class assertion in addition to all three displayed relational
conditions. The present ✱122 kernel does not encode that class, so a shorter
existential would be a genuine weakening. ✱123·13 requires PM cardinal-number
formation and cardinal addition, neither of which is supplied by the natural
segment kernels.

The remaining propositions likewise cross an unavailable semantic boundary:
✱123·14 combines inductive cardinals, `Clʻα`, intersection, and existence;
✱123·15 asserts non-membership in the PM class of inductive classes; and
✱123·16 relates `Clʻα` to the union of that class with `ℵ₀`. Introducing opaque
predicates or adding these conclusions as hypotheses would not formalize the
printed claims. All five therefore remain `prepared` with explicit blocked
statuses in the homogeneous refusal lot.

The printed references ✱122·35, ·37, and ·46 are preserved diplomatically but
are not placed in `normalized_dependencies`, because those three loci do not
yet exist as catalogued metadata nodes. This avoids manufacturing resolved
edges in the dependency graph.
