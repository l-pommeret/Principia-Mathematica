# ✱35 catalogue 01 — Star2 object-kernel gate

This v1 gate covers exactly PM1:✱35·01, ·02, ·03, ·04, and ·05. The
five catalogue readings agree with their literal source blocks. They are PM
definitions of left restriction, right restriction, simultaneous restriction,
the Cartesian relation, and the derived `Rʻx` Cartesian construction.

## Formal endpoint audit

All five records are source-only. Their `lean_path` points to the diplomatic
source file and each declared `source_only_*` name is intentionally absent.
Consequently there is no Lean object-language `Formula`, no printed `Assertion`,
and no kernel declaration or derivation comparable to the Star2 standard.

The predicate-level functions in `Star35ConsecutiveKernel.lean` are useful
secondary semantics, but they do not by themselves supply a PM syntax object or
a kernel-checked PM judgment. They therefore cannot promote these definitions
under the required gate. All five records remain `prepared` and are blocked as
`blocked-missing-pm-object-kernel`.

## Dependency graphs rebuilt from zero

The five definitions print no numbered citations, so every printed dependency
graph is empty. Because no Lean declaration body exists, there are no PM theorem
calls to extract; every Lean-call graph is empty. Normalization of the empty
graphs is empty. These are absence results for blocked source-only items, not
evidence of formal completion.
