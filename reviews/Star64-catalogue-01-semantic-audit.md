# PM I ✱64 catalogue 01 strict semantic audit

All five definitions are refused and remain `prepared`. Their printed targets
construct distinct relative relation types from ordered operand types. Lean's
`architectureOnly_64_*` labels ultimately reduce every target to `t x` in one
carrier, preserving neither operand levels nor the `↑` construction.

Direct parser tests fail on `↑` in all five unchanged strings, validating
their existing `reviewed-gap`. Each locus has one record and one source block;
no split or refusal sidecar is introduced.
