# ✱40 catalogue 07 — cumulative T1–T9 object gate

This cumulative gate covers PM1:✱40·4, ·41, ·42, ·43, and ·44. All five
source readings match their literal blocks.

None has a primary PM object endpoint. The declarations in
`Star40FinalKernel.lean` conclude typed predicate/class equalities or
equivalences in `Prop`; they do not state a syntax-object judgment and do not
construct an axiom-free PM derivation. The first four formerly accepted
predicate endpoints (·4, ·41, ·43, ·44) are therefore returned to `prepared`
and blocked as `blocked-missing-axiom-free-pm-object-proof`. Item ·42 remains
blocked for the stronger independent reason that its Lean endpoint omits PM's
middle equality; it also lacks an object derivation.

No declaration uses a printed-definition constructor, `Support`, an axiom, or
a pass-through premise as a certification endpoint. This does not promote them:
the missing object judgment is decisive.

## Graphs rebuilt from zero

The five displayed source blocks contain no numbered dependency citation.
Their theorem bodies call no numbered PM theorem. Thus the printed, Lean-call,
and normalized graphs are empty for every item. The empty graphs describe the
actual bodies and do not imply v1 completion.

