# PM I ✱64 catalogue 03 strict semantic audit

All five items are refused and remain `prepared`. Definitions ·04 and ·041
again collapse heterogeneous relative relation types to `t x`. Propositions
·1, ·11, and ·12 mention relation objects and operand type levels absent
from the Lean signatures; their namesakes are only reflexive facts in the
single-carrier `Same` model. They are not typed renderings of the source.

All five unchanged strings fail the parser on `↑`, matching their existing
reviewed gaps. Source blocks are unique and no refusal split is created.
