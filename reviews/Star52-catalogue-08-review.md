# PM I ✱52 catalogue-08 strict semantic audit

✱52·7 is exact.  From the hypothesis that `β − α` is a unit class and
the inclusions `α ⊂ ξ ⊂ β`, Lean proves the complete printed dichotomy
`ξ = α ∨ ξ = β`; neither branch is assumed and no finiteness or decidability
hypothesis is added.  The item is therefore `awaiting-ci`.  Its diplomatic
reading parses directly and needs no parser-gap classification.
