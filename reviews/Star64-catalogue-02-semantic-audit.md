# PM I ✱64 catalogue 02 strict semantic audit

All five definitions are refused and remain `prepared`. Despite suggestive
names, `t01`, `t10`, `t0one`, and `t1one` each have body `t x`; they omit the
printed ordered operand levels and relative relation-type constructor. The
remaining `architectureOnly_64_022` is explicitly a non-faithful one-carrier
label. None is exact without genuine heterogeneous type architecture.

Direct parser tests fail on `↑` in all five unchanged strings, validating
their existing `reviewed-gap`. Graphs remain empty because no declaration is
accepted. Each ID has one record, with no split sidecar.
