# ✱121 catalogue 01 strict semantic audit

The five literal source blocks PM2:✱121·01, ·011, ·012, ·013, and ·02
match their catalogue records and the declarations in
`Principia/Architecture/Star121OpeningKernel.lean`.

All five translations pass strict equivalence. The four interval definitions
are represented as characteristic predicates and preserve, in order, PM's
strict/strict, strict/reflexive, reflexive/strict, and reflexive/reflexive
endpoint conditions. In ·02, `rank x y` represents the displayed cardinal
measure `N₀cʻP(x⊢⊣y)` and Lean's `n + 1` represents `ν+_c1`; the defining
equality and both bound variables are retained.

These are primitive definitions and print no numbered dependencies. Their Lean
proofs are judgmental unfoldings of the corresponding local definitions, so
the empty Lean dependency graphs are exact rather than relaxed. No additional
assumption or theorem is introduced, and no item is refused. All five records
are promoted in place to `awaiting-ci`; CI evidence remains pending.
