# Q311 canonical integration

All three targets use the already audited contextual expansion from
`Star14Q299Kernel`; no description becomes a term. ✱14·3 takes as an explicit
argument exactly its printed antecedent saying that `f` respects proposition
equivalence. It therefore needs neither an unrestricted proposition-variable
rule nor a new reducibility axiom. Given the unique candidate supplied by
description existence, both sides reduce to `f (χ b)`.

✱14·31 distributes an independent proposition through the same unique
candidate. ✱14·32 is proved in both directions, including the non-denoting
case: if no candidate exists, both contextual scopes are false, so the inner
equivalence is false as required. The reverse implication of ✱14·32 uses
Lean's theorem of classical contradiction, matching PM's classical elementary
logic; it is not a new axiom. The proofs introduce no choice, default
description value, unsafe declaration, or generic assertion rule. Direct
✱14·01 expansion compresses the longer printed
routes; the metadata records those unused historical citations explicitly.

The primary object-calculus declaration of ✱14·31 now lives in
`Star14Derived.lean`. Its public theorem delegates to a private strict proof:
the two ✱14·242 instances give the candidate-level equivalences, ✱4·37 lifts
the second through the independent disjunct (with the explicit commutations
required by the ramified orientation), and primitive generalization produces
the ✱10·23 real-scope normal form. ✱14·11 then records the printed existence
expansion. The only remaining premise is the polymorphic, explicitly named
`star_14_242_hypothesis`; it is strictly narrower than assuming ✱14·31 itself.
