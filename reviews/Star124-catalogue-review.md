# ✱124 source catalogue review

All sixty loci are checked against Project Gutenberg 78255 on printed pages
280–288 (scan leaves 320–328). Source IDs and homonymous Lean declarations form
a duplicate-free nominal 60/60 mapping. The source was moved from the erroneous
`SecondEdition` tree into first-edition Volume II, matching its 1912 witness.

Every item remains `prepared`: the existing typed cardinal reconstruction has
not yet passed item-level semantic promotion. The parser does not support these
cardinal and progression formulas, so the entries carry `reviewed-gap`.
