# Q304 canonical integration

The four propositions are proved after the exact ✱14·01 contextual expansion
already isolated by `Star14Q299Kernel`. `EqualDescription a φ` means that a
unique candidate characterized by `φ` exists and equals `a`;
`DescriptionsEqual φ ψ` expands both scopes and compares their candidates.
No description is introduced as a Lean term.

The proofs compose the resulting candidate equalities and, when two
independent expansions characterize the same predicate, identify their
candidates from the two characterization equivalences. This directly closes
✱14·14, ·142, ·144, and ·145 constructively. It introduces no choice,
classical axiom, default value, unsafe declaration, or description operator.
The longer printed derivation chains are historically faithful routes through
earlier PM propositions; the kernel proofs instead normalize their common
✱14·01 definiens, and this review records that deliberate closure compression.
