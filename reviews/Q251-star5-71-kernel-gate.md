# Gate kernel ✱5·71 — PM I, p. 131

Candidate awaiting CI. Under `q ⊃ ∼r`, transposition ✱2·16 and double negation ✱2·12 yield `r ⊃ ∼q`; ✱2·55 then eliminates the `q` branch of `p ∨ q`. The converse adds `q` to `p`, and both directions retain `r` by explicit conjunction reconstruction. No generic detachment, Aristotle-local interface, oracle, axiom, placeholder, semantics, native connective, or source correction is used.
