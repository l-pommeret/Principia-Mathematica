# ✱37 catalogue-13 semantic audit

All five entries are refused for formal promotion.  ✱37·323, ·34, ·341, ·35 and
·351 are preserved only as diplomatic `PM-VERBATIM` source blocks in
`Star37Source.lean`; none has a Lean declaration, theorem target, or proof to
compare with the printed statement.

The batch is therefore homogeneous: `formal_status` stays `prepared` and
`integration_status` is `blocked-no-lean-declaration` for every item.  In
particular, the printed image-relation and iterated-image equations are not
weakened to nearby typed results merely to obtain promotable metadata.
