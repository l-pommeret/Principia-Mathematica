# ✱212 canonical review

The first homogeneous batch is ✱212·01, ·02, ·1, ·11, ·12. The source blocks
are complete and ordered. Declarations ·01, ·02, and ·1 are only abstractions:
they erase essential dependence on `P` (and ·1 erases the image subtraction),
so they remain `prepared`.

For classes represented as predicates, `Proper a b` is equivalent to the PM
condition that `b-a` exists. Thus ·11 is exact after specializing the printed
carrier, and ·12 is the identical statement on `sectʻP`; only these two are
promoted to `awaiting-ci`. No other ✱212 item is promoted here.
