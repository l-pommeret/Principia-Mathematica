# Gate kernel ✱4·73

Remote kernel certification: run `31558455470` succeeded at commit `495b76cb90a0f79667f5afff11c6c85af7cce653` (aggregate preflight, isolated contexts, and complete edition build).

The canonical facsimile, p. 127 / leaf 149, gives `q ⊃ : p ≡ p . q`, followed by the explanatory paragraph about omitting a true factor. The candidate keeps that orientation. ✱3·21 gives `q ⊃ p ⊃ p·q`; the first projection is lifted under `q`; and explicit duplication plus ✱3·47 package the two directions. No generic detach, oracle, or altered factor order is used.

Static local gate: `PATH=/Users/user/.local/lean-4.30.0/bin:$PATH lake build Principia.FirstEdition.Volume1.Part1.SectionA.Star4` succeeded in 3.5 s.
