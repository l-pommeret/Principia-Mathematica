# Gate kernel ✱5·44

Kernel-checked by CI run `31554037055` at commit `bc34b63d279a21b730eb17beffe8d4512f037921`. Under `p ⊃ q`, the forward direction projects the two conditionals and `p` from an explicit conjunction, derives `q` and `r`, then curries the resulting conjunction. The reverse direction lifts the projection `(q · r) ⊃ r` through `p` with ✱2·05. The two directions are paired under the same hypothesis using only explicit inference branches; no generic detach rule or oracle is used.
