# Gate kernel ✱5·44

Candidate awaiting CI. Under `p ⊃ q`, the forward direction projects the two conditionals and `p` from an explicit conjunction, derives `q` and `r`, then curries the resulting conjunction. The reverse direction lifts the projection `(q · r) ⊃ r` through `p` with ✱2·05. The two directions are paired under the same hypothesis using only explicit inference branches; no generic detach rule or oracle is used.
