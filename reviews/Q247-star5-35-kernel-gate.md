# Gate kernel ✱5·35 — PM I, p. 130

Kernel-checked in CI run `31550205447` at commit `36a065210e92dd463dc6b1298cc7c13042692b63`. The printed `[Comp . ✱5·1]` is retained and expanded through the accepted kernel only: obtain `q` and `r` from the two implications under `p`, join them, then apply ✱5·1. All inference branches are explicit; no generic detachment, oracle, axiom, placeholder, semantics, native connective, or source correction occurs.
