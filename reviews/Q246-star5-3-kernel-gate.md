# Gate kernel ✱5·3

Kernel-checked by CI run `31554037055` at commit `bc34b63d279a21b730eb17beffe8d4512f037921`. From the common antecedent `p · q`, the forward direction pairs its first projection with the supplied conclusion `r`; the reverse direction lifts the projection `(p · r) ⊃ r`. Both directions use explicit primitive inference branches, then ✱3·2 packages the defined equivalence. No generic detachment or oracle is used.
