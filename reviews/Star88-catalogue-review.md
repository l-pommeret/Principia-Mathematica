# ✱88 source catalogue review

The thirty-five loci are checked against Project Gutenberg 78050 on printed
pages 562–568 (scan leaves 584–590). The three definitions map to the typed
`Multipliable`/`MultiplicativeAxiom` reconstruction; the remaining source IDs
map uniquely to numbered declarations. Auxiliary theorem IDs ·35/·36/·361 have
no source locus and are excluded. All items remain `prepared` pending semantic
promotion, especially the typically ambiguous reuse of `Multipliable` for ·01/·02.

The parser accepts 0 formulas; 35 selection/choice formulas carry `reviewed-gap`.
