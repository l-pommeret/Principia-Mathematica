# Q245 kernel-link continuation

Return only the target theorem bodies for Q245 against the exact declarations already supplied by the audited context and manifest.  Import and reference only the listed real kernel constants; do not define modules, helper systems, local copies, axioms, `sorry`, `admit`, `unsafe`, or `Classical`.  If a one-to-one kernel remap is unavailable, report the blocking declaration per target instead of inventing one.  This is interface validation only and confers no canonical promotion.
