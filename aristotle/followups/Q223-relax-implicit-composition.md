# Q223 authorized minimal-relaxation continuation

The preceding strict retry established a kernel-checked obstruction: the
printed chains for `✱3·27` and `✱3·31` each require exactly one composition
step. Editorial authorization now permits **only**
`PM.FirstEdition.Volume1.Star1.star_1_6` (`PM.Derivation.star_1_6`) at those
two loci, classified as `incomplete-printed-citation/implicit-composition-gap`.

Produce the four exact Q223 declarations, retaining the accepted strict bodies
of `star_3_26` and `star_3_3`. For `star_3_27` and `star_3_31`, use exactly one
occurrence of `star_1_6` to compose the two printed premises; retain all
printed events and substitutions. No other relaxation or non-whitelisted
theorem is permitted. `detach` remains available only under the already
reviewed explicit convention; do not add a new convention.

Do not change theorem statements or add helpers, axioms, `Classical`,
semantics, `sorry`, `admit`, `unsafe`, or alternate notation. Return only the
requested declarations, in order, with each dependency apparent in its proof
term. The final audit must be able to account for exactly one added `✱1·6` in
each of `✱3·27` and `✱3·31`, and none elsewhere.
