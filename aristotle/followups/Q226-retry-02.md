# Q226 continuation — resolved printed aliases and minimal context-polymorphism exception

The immediately preceding retry is diagnostic only.  Reconstruct all four
original declarations in the stated order in the same project, using the
regenerated manifest and this instruction as the complete policy.

## Exact permissions

* For **✱3·45**, the printed `Syll` event resolves only to the kernel items
  `PM1:✱3·33` or `PM1:✱3·34`, selecting the one whose displayed conjunction
  premise matches the line being composed.  `Transp` resolves to the matching
  whitelisted `✱2·15`/`✱2·16`/`✱2·17`, and `Id` to `✱2·08`.  The event list is
  precisely `Syll`, `Transp`, `Id`, `✱1·01`, `✱3·01`: **do not introduce
  `Comm`** for this target.
* For **✱3·47**, `Fact` is exactly the preceding local result `✱3·45`; its
  two printed `✱3·03` uses remain required.  The sole exceptional permission
  is `PM1:✱3·2`, classified `context-polymorphism-gap`, and may be used only
  in the `Γ = []` branch to provide the adjunction shape that printed `✱3·03`
  provides when `Γ ≠ []`.  In the nonempty branch use `✱3·03`.  No other
  extra item, rule, or relaxation is authorized.

## Literal source hygiene

Do not copy the prior generated `PMContext.lean` or any old self-contained
foundation block: it embeds old uses of `PM.Derivation.star_1_5` and
`PM.Derivation.star_1_6`.  Use the existing audited project declarations via
imports/references instead, and audit **every delivered Lean file**, including
any context/support file.  There must be no occurrence in executable source of
`PM.Derivation.star_1_5`, `PM.Derivation.star_1_6`, `syllRuleQ220`, an
unlisted primitive/Star theorem, `axiom`, `sorry`, `admit`, `unsafe`, or
`Classical`.  The only primitive inference is the manifest's explicit
detachment convention.  `Comm`, where printed in other targets, is the
theorem `PM.FirstEdition.Volume1.Star2.star_2_04`, never primitive ✱1·5.

Return a compact source-audit ledger showing the selected kernel item for
every printed event, the two Γ branches for the sole `✱3·2` exception, and a
literal forbidden-token scan over every submitted Lean file.  No semantic
model/obstruction replaces any target in this continuation.
