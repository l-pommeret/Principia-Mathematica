# Q200 — Prove the faithful metalinguistic detachment lemma

Formalize the single declaration below in Lean 4.30.0. It is the complete
scope of Q200. Solve the exact target without adding, deleting, weakening, or
re-parenthesizing any parameter or conclusion.

This prompt is self-contained. Use exactly the following definitions, which
reproduce the project's elementary syntax and deductive system through ✱1·6:

```lean
namespace PM

inductive RealType where
  | elementaryProposition : RealType
  deriving DecidableEq, Repr

abbrev RealContext := List RealType

inductive RealVar : (Γ : RealContext) → RealType → Type where
  | zero : RealVar (τ :: Γ) τ
  | succ : RealVar Γ τ → RealVar (σ :: Γ) τ
  deriving DecidableEq, Repr

inductive Elementary : RealContext → Type where
  | constant : String → Elementary Γ
  | var : RealVar Γ .elementaryProposition → Elementary Γ
  | neg : Elementary Γ → Elementary Γ
  | disj : Elementary Γ → Elementary Γ → Elementary Γ
  deriving DecidableEq, Repr

namespace Elementary

prefix:max "∼ₚ" => neg
infixr:55 " ∨ₚ " => disj

/-- PM ✱1·01: `p ⊃ q .=. ∼p ∨ q Df`. -/
def imp (p q : Elementary Γ) : Elementary Γ := disj (neg p) q

infixr:54 " ⊃ₚ " => imp

end Elementary

inductive Derivation : {Γ : RealContext} → Elementary Γ → Prop where
  /-- PM ✱1·1: inference between asserted definite elementary propositions. -/
  | star_1_1 {p q : Elementary []} :
      Derivation p → Derivation (p ⊃ₚ q) → Derivation q
  /-- PM ✱1·11: inference between asserted functions with real variables. -/
  | star_1_11 {Γ : RealContext} {φ ψ : Elementary Γ}
      (hasRealVariable : Γ ≠ []) :
      Derivation φ → Derivation (φ ⊃ₚ ψ) → Derivation ψ
  /-- PM ✱1·2, Taut. -/
  | star_1_2 {Γ : RealContext} (p : Elementary Γ) :
      Derivation ((p ∨ₚ p) ⊃ₚ p)
  /-- PM ✱1·3, Add. -/
  | star_1_3 {Γ : RealContext} (p q : Elementary Γ) :
      Derivation (q ⊃ₚ (p ∨ₚ q))
  /-- PM ✱1·4, Perm. -/
  | star_1_4 {Γ : RealContext} (p q : Elementary Γ) :
      Derivation ((p ∨ₚ q) ⊃ₚ (q ∨ₚ p))
  /-- PM ✱1·5, Assoc. -/
  | star_1_5 {Γ : RealContext} (p q r : Elementary Γ) :
      Derivation ((p ∨ₚ (q ∨ₚ r)) ⊃ₚ (q ∨ₚ (p ∨ₚ r)))
  /-- PM ✱1·6, Sum. -/
  | star_1_6 {Γ : RealContext} (p q r : Elementary Γ) :
      Derivation ((q ⊃ₚ r) ⊃ₚ ((p ∨ₚ q) ⊃ₚ (p ∨ₚ r)))

notation:45 "⊢ₚ " p => Derivation p

end PM
```

The exact target is:

```lean
namespace PM.Derivation

theorem detach {Γ : PM.RealContext} {φ ψ : PM.Elementary Γ}
    (hφ : PM.Derivation φ) (hφψ : PM.Derivation (φ ⊃ₚ ψ)) :
    PM.Derivation ψ := by
  -- prove exactly this target

end PM.Derivation
```

Historical and formal obligations:

- Analyze only the shape of `Γ`.
- In the `Γ = []` branch, use exactly `PM.Derivation.star_1_1` (PM ✱1·1).
- In the `Γ = τ :: Δ` branch, use exactly
  `PM.Derivation.star_1_11` (PM ✱1·11), supplying only the structural proof
  that `τ :: Δ ≠ []`.
- Preserve the distinction between a definite elementary proposition and an
  elementary propositional function with one or more real variables.
- The proof must work for the dependent expressions `φ ψ : PM.Elementary Γ`;
  if dependent elimination requires generalizing or reverting them, do so
  without changing the theorem statement.

Constraints:

- Do not use any constructor except `star_1_1` and `star_1_11` in the proof.
- Do not interpret `PM.Elementary` as Lean `Prop` and do not prove semantic
  soundness or a truth-functional modus ponens theorem.
- Do not use `Classical`, truth tables, `by_cases` on a formula, excluded
  middle, proof irrelevance, semantic automation, or generic object-language
  substitution.
- Do not add an axiom, inference constructor, constant, typeclass assumption,
  hypothesis, helper postulate, `sorry`, `admit`, or unsafe declaration.
- Do not weaken or remove the `Γ ≠ []` premise of `star_1_11`.
- Do not treat `Γ` as a context of logical hypotheses; it records real
  variables only.
- Preserve the namespace, theorem name, implicit parameters, explicit proof
  arguments, result, and exact parenthesization.

Return one standalone Lean file containing the displayed definitions and the
proved declaration. After the code, add a short plain-text note saying which
PM primitive rule is used in each of the two context branches.
