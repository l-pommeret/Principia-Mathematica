# Q300 — PM I ✱9·21, exact assigned 1→2 derivation

Submission gate: the supplied Q300 context must be compiled remotely as a
standalone file before attempting the target. This is an interface-gated
architecture proof: a successful sandbox result is not canonical integration
until every local architecture declaration is remapped one-to-one to accepted
repository declarations.

Canonical source: first edition, vol. I (1910), p. 139, scan leaf 161,
SHA-256 `6897362dc21208f4c3f698fe66ff4e45e40ea9ed3742647bf50afdabda8de8fa`.
Use the complete diplomatic proof in `aristotle/demonstrations/PM1-star-9-21.txt`.

Return exactly this declaration:

```lean
namespace PM.Architecture.FirstOrderPrerequisites

theorem star_9_21
    (φ ψ : Apparent Γ [.elementaryProposition]) :
    Star_9_21Derivation φ ψ := by
  -- eight printed lines of PM I ✱9·21, not a semantic monotonicity proof

end PM.Architecture.FirstOrderPrerequisites
```

The mandatory dependency path is fixed by the printed lines: `✱2·08`; twice
`✱9·1`; exactly once the specialized `✱9·13` constructor
`OrderedAssertion.star_9_13_first`; then only reductions `✱9·06`, `✱1·01`,
`✱9·08`, `✱9·08`, `✱1·01`. `✱1·11`/`✱9·12` are the standing detachment
conventions where the printed display omits them.

The line-(4) assertion must pass through the sealed `MatrixSyntaxAt 1`
instance and `firstOrderToSecondAll`; it may not call a general order-indexed
rule. Close the final `y` and `z` binders only through
`closedFirstOrderAlphaRenaming` and the structural
`openRealHead_abstractRealHead` reduction. Do not use alpha conversion as an
unstated definitional replacement.

Do not add a target constructor, an axiom, an oracle, `Classical`, object
`Prop`, a semantic quantifier theorem, generic substitution, or a second
`MatrixSyntaxAt` instance. Return no helpers beyond local `have` statements.
