# Q214 — Formalize PM I ✱2·6–✱2·621 exactly

**Submission gate: PREPARED ONLY.** Submit only after Q209 and Q213 are
integrated and kernel-checked by GitHub CI. Replace the reproduced dependency
bodies with the exact accepted bodies before submission.

Canonical scan: first edition, vol. I, p. 112, [leaf 134](https://en.wikisource.org/wiki/Page:Russell,_Whitehead_-_Principia_Mathematica,_vol._I,_1910.djvu/134).

Return one standalone Lean 4.30.0 file containing the full project definitions,
proved `detach`, and proved bodies for `star_2_04`, `star_2_05`, `star_2_38`,
and `star_2_53`. Prove exactly:

```lean
namespace PM.FirstEdition.Volume1.Star2
theorem star_2_6 {Γ} (p q : PM.Elementary Γ) :
    ⊢ₚ ((∼ₚ p ⊃ₚ q) ⊃ₚ ((p ⊃ₚ q) ⊃ₚ q)) := by
  -- preserve printed lines (1), (2), then Syll
theorem star_2_61 {Γ} (p q : PM.Elementary Γ) :
    ⊢ₚ ((p ⊃ₚ q) ⊃ₚ ((∼ₚ p ⊃ₚ q) ⊃ₚ q)) := by
  -- [✱2·6.Comm]
theorem star_2_62 {Γ} (p q : PM.Elementary Γ) :
    ⊢ₚ ((p ∨ₚ q) ⊃ₚ ((p ⊃ₚ q) ⊃ₚ q)) := by
  -- [✱2·53·6.Syll]
theorem star_2_621 {Γ} (p q : PM.Elementary Γ) :
    ⊢ₚ ((p ⊃ₚ q) ⊃ₚ ((p ∨ₚ q) ⊃ₚ q)) := by
  -- [✱2·62.Comm]
end PM.FirstEdition.Volume1.Star2
```

For ✱2·6 first obtain
`(∼p⊃q) ⊃ ((∼p∨q) ⊃ (q∨q))` from ✱2·38. Lift Taut by the exact Syll body to
obtain `((∼p∨q)⊃q∨q) ⊃ ((∼p∨q)⊃q)`, then compose and only then read
`∼p∨q` as `p⊃q`. ✱2·61 and ✱2·621 use the real ✱2·04 body; ✱2·62 composes
the real ✱2·53 and ✱2·6 bodies. No primitive Syll, generic composition axiom,
semantics, Classical, `sorry`, `admit`, `unsafe`, or scope changes.

