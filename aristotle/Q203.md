# Q203 — Formalize PM I ✱2·06–✱2·1 exactly

**Submission gate:** this prompt is prepared but MUST NOT be submitted until
the project has kernel-checked and integrated Q200 (`PM.Derivation.detach`)
and Q202 (`star_2_03`–`star_2_05`) in GitHub CI. They are reproduced with
proof bodies below to make the task autonomous; they are prior proved
declarations, not new axioms or goals.

Canonical source: Whitehead and Russell, *Principia Mathematica*, first
edition, volume I (1910), printed pp. 104–105, scan leaves 126–127:

- https://en.wikisource.org/wiki/Page:Russell,_Whitehead_-_Principia_Mathematica,_vol._I,_1910.djvu/126
- https://en.wikisource.org/wiki/Page:Russell,_Whitehead_-_Principia_Mathematica,_vol._I,_1910.djvu/127

Formalize all four declarations below in Lean 4.30.0. They are the complete
scope of Q203. Solve every target without adding, deleting, weakening, or
re-parenthesizing parameters or conclusions.

## Complete formal context

Use exactly this syntax and primitive deductive system:

```lean
namespace PM

inductive RealType where
  | elementaryProposition : RealType

abbrev RealContext := List RealType

inductive RealVar : (Γ : RealContext) → RealType → Type where
  | zero : RealVar (τ :: Γ) τ
  | succ : RealVar Γ τ → RealVar (σ :: Γ) τ

inductive Elementary : RealContext → Type where
  | constant : String → Elementary Γ
  | var : RealVar Γ .elementaryProposition → Elementary Γ
  | neg : Elementary Γ → Elementary Γ
  | disj : Elementary Γ → Elementary Γ → Elementary Γ

namespace Elementary
prefix:max "∼ₚ" => neg
infixr:55 " ∨ₚ " => disj

/-- PM ✱1·01: `p ⊃ q .=. ∼p ∨ q Df`. -/
def imp (p q : Elementary Γ) : Elementary Γ := disj (neg p) q
infixr:54 " ⊃ₚ " => imp
end Elementary

inductive Derivation : {Γ : RealContext} → Elementary Γ → Prop where
  | star_1_1 {p q : Elementary []} :
      Derivation p → Derivation (p ⊃ₚ q) → Derivation q
  | star_1_11 {Γ : RealContext} {φ ψ : Elementary Γ}
      (hasRealVariable : Γ ≠ []) :
      Derivation φ → Derivation (φ ⊃ₚ ψ) → Derivation ψ
  | star_1_2 {Γ : RealContext} (p : Elementary Γ) :
      Derivation ((p ∨ₚ p) ⊃ₚ p)
  | star_1_3 {Γ : RealContext} (p q : Elementary Γ) :
      Derivation (q ⊃ₚ (p ∨ₚ q))
  | star_1_4 {Γ : RealContext} (p q : Elementary Γ) :
      Derivation ((p ∨ₚ q) ⊃ₚ (q ∨ₚ p))
  | star_1_5 {Γ : RealContext} (p q r : Elementary Γ) :
      Derivation ((p ∨ₚ (q ∨ₚ r)) ⊃ₚ (q ∨ₚ (p ∨ₚ r)))
  | star_1_6 {Γ : RealContext} (p q r : Elementary Γ) :
      Derivation ((q ⊃ₚ r) ⊃ₚ ((p ∨ₚ q) ⊃ₚ (p ∨ₚ r)))

notation:45 "⊢ₚ " p => Derivation p

namespace Derivation

/-- Previously proved metalinguistic bridge Q200; not an axiom. -/
theorem detach {Γ : PM.RealContext} {φ ψ : PM.Elementary Γ}
    (hφ : PM.Derivation φ) (hφψ : PM.Derivation (φ ⊃ₚ ψ)) :
    PM.Derivation ψ := by
  cases Γ with
  | nil => exact PM.Derivation.star_1_1 hφ hφψ
  | cons τ Δ =>
      exact PM.Derivation.star_1_11 (by simp) hφ hφψ

end Derivation
end PM
```

The following are prior Q202 theorems. Keep these proof bodies; do not turn
them into axioms, hypotheses, constructors, or new goals:

```lean
namespace PM.FirstEdition.Volume1.Star2

theorem star_2_03 {Γ : PM.RealContext} (p q : PM.Elementary Γ) :
    ⊢ₚ ((p ⊃ₚ ∼ₚ q) ⊃ₚ (q ⊃ₚ ∼ₚ p)) := by
  exact PM.Derivation.star_1_4 (∼ₚ p) (∼ₚ q)

theorem star_2_04 {Γ : PM.RealContext} (p q r : PM.Elementary Γ) :
    ⊢ₚ ((p ⊃ₚ (q ⊃ₚ r)) ⊃ₚ (q ⊃ₚ (p ⊃ₚ r))) := by
  exact PM.Derivation.star_1_5 (∼ₚ p) (∼ₚ q) r

theorem star_2_05 {Γ : PM.RealContext} (p q r : PM.Elementary Γ) :
    ⊢ₚ ((q ⊃ₚ r) ⊃ₚ ((p ⊃ₚ q) ⊃ₚ (p ⊃ₚ r))) := by
  exact PM.Derivation.star_1_6 (∼ₚ p) q r

end PM.FirstEdition.Volume1.Star2
```

## Exact historical source and obligations

The diplomatic PM text for ✱2·06 is:

```text
✱2·06.  ⊢ :. p ⊃ q . ⊃ : q ⊃ r . ⊃ . p ⊃ r

Dem.

        [Comm  (q ⊃ r, p ⊃ q, p ⊃ r)/(p, q, r)]
              ⊢ :: q ⊃ r . ⊃ : p ⊃ q . ⊃ . p ⊃ r :.
                   ⊃ :. p ⊃ q . ⊃ : q ⊃ r . ⊃ . p ⊃ r       (1)
        [✱2·05]  ⊢ :. q ⊃ r . ⊃ : p ⊃ q . ⊃ . p ⊃ r        (2)
        [(1).(2).✱1·11]
              ⊢ :. p ⊃ q . ⊃ : q ⊃ r . ⊃ . p ⊃ r
```

Thus instantiate ✱2·04 (`Comm`) with, in order,
`q ⊃ₚ r`, `p ⊃ₚ q`, `p ⊃ₚ r`. This yields an implication from the exact
✱2·05 formula to the requested reordered formula. Detach the already proved
✱2·05 instance with `PM.Derivation.detach`. This is PM's printed use of
✱1·1/✱1·11, not an extra object-language rule.

PM then says exactly:

> In the last line of this proof, “(1).(2).✱1·11” means that we are inferring
> in accordance with ✱1·11, having before us a proposition, namely
> `p ⊃ q . ⊃ : q ⊃ r . ⊃ . p ⊃ r`, which, by (1), is implied by
> `q ⊃ r . ⊃ : p ⊃ q . ⊃ . p ⊃ r`, which, by (2), is true. In general, in
> such cases, we shall omit the reference to ✱1·11.
>
> The above two propositions will both be referred to as the “principle of
> the syllogism” (shortened to “Syll.”), because, as will appear later, the
> syllogism in Barbara is derived from them.

For ✱2·07 the complete printed item and explanation are:

```text
✱2·07.  ⊢ : p . ⊃ . p ∨ p                    [✱1·3  p/q]
```

> Here we put nothing beyond “✱1·3 p/q,” because the proposition to be proved
> is what ✱1·3 becomes when p is written in place of q.

Use exactly the direct ✱1·3 (`Add`) instance whose two Lean arguments are `p`.

For ✱2·08 PM prints:

```text
✱2·08.  ⊢ . p ⊃ p

Dem.

        [✱2·05  (p ∨ p, p)/(q, r)]
              ⊢ :: p ∨ p . ⊃ . p : ⊃ :. p . ⊃ . p ∨ p : ⊃ . p ⊃ p   (1)
        [Taut]  ⊢ : p ∨ p . ⊃ . p                                   (2)
        [(1).(2).✱1·11]
              ⊢ :. p . ⊃ . p ∨ p : ⊃ . p ⊃ p                       (3)
        [2·07]  ⊢ : p . ⊃ . p ∨ p                                  (4)
        [(3).(4).✱1·11]  ⊢ . p ⊃ p
```

Instantiate ✱2·05 with `p`, `p ∨ₚ p`, `p` in that order. Detach Taut
(`star_1_2 p`) from this line, then detach ✱2·07. Preserve both detachment
steps and their order; do not replace the historical demonstration by a
modern identity theorem.

Finally PM prints:

```text
✱2·1.   ⊢ . ∼p ∨ p                              [Id. (✱1·01)]
```

This is only the definitional ✱1·01 reading of ✱2·08. Apply `star_2_08 p` and
let `PM.Elementary.imp` reduce definitionally. Do not invoke excluded middle
or semantic reasoning. (The printed law of excluded middle is the later
✱2·11, outside this batch.)

## Exact targets and internal order

Prove these declarations in precisely this order:

```lean
namespace PM.FirstEdition.Volume1.Star2

theorem star_2_06 {Γ : PM.RealContext} (p q r : PM.Elementary Γ) :
    ⊢ₚ ((p ⊃ₚ q) ⊃ₚ ((q ⊃ₚ r) ⊃ₚ (p ⊃ₚ r))) := by
  -- exact historical reconstruction above

theorem star_2_07 {Γ : PM.RealContext} (p : PM.Elementary Γ) :
    ⊢ₚ (p ⊃ₚ (p ∨ₚ p)) := by
  -- direct Add instance

theorem star_2_08 {Γ : PM.RealContext} (p : PM.Elementary Γ) :
    ⊢ₚ (p ⊃ₚ p) := by
  -- ✱2·05, then Taut detachment, then ✱2·07 detachment

theorem star_2_1 {Γ : PM.RealContext} (p : PM.Elementary Γ) :
    ⊢ₚ (∼ₚ p ∨ₚ p) := by
  -- definitional ✱1·01 reading of ✱2·08

end PM.FirstEdition.Volume1.Star2
```

## Mandatory constraints

- Use only the displayed primitives, the proved `PM.Derivation.detach`, the
  displayed prior Q202 theorem bodies, and earlier declarations from this
  Q203 batch in the dependency order stated above.
- Do not use `star_2_03` merely because it is present; it is included only to
  make the prior Q202 block complete.
- Preserve every `Γ`, variable, theorem name, connective, and parenthesis.
- Do not interpret `PM.Elementary` as Lean `Prop`; do not use truth tables,
  `Classical`, `by_cases`, semantic soundness, excluded middle, associativity
  or commutativity lemmas, or generic object-language substitution.
- Do not introduce axioms, new constructors, constants, postulates,
  hypotheses, typeclass assumptions, alternate statements, `sorry`, `admit`,
  unsafe declarations, or theorem aliases that bypass a target.
- Do not add a generic modus ponens rule. `detach` must remain the proved
  metalinguistic case split between PM ✱1·1 and ✱1·11 shown above.
- Return one standalone Lean file containing the displayed definitions, the
  proved prior declarations with their bodies, and all four new proofs. After
  the code, give a short plain-text dependency trace for each new theorem.
