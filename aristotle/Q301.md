# Q301 — PM I ✱9·23 and ✱9·25 after an audited ✱9·21 remap

Submission gate: submit only after Q300 is terminal, audited, and remapped
one-to-one to a kernel-checked `PM.Architecture.FirstOrderPrerequisites.star_9_21`.
Before that remap this batch has no Lean context: its recorded Q300 signature
is a dependency contract, not an executable axiom or an asserted target.

Canonical source: first edition, vol. I (1910), p. 140, scan leaf 162,
SHA-256 `bd0e3af38b946e64c1f6d9a59d95d427ff9dac0fef26f9f7f7898896ea1ddcab`.

Produce these declarations in order:

```lean
namespace PM.Architecture.FirstOrderPrerequisites

theorem star_9_23
    (φ : Apparent Γ [.elementaryProposition]) :
    OrderedAssertion (star_9_23_target φ) := by
  -- [Id.✱9·13·21]

theorem star_9_25
    (p : Elementary Γ) (φ : Apparent Γ [.elementaryProposition]) :
    Star_9_25Derivation p φ := by
  -- [Id.✱9·23.(✱9·04)]

end PM.Architecture.FirstOrderPrerequisites
```

`star_9_23` must discharge its elementary identity internally using the exact
kernel theorem `PM.FirstEdition.Volume1.Star2.star_2_08` at
`Apparent.openHead (matrixImp φ φ)`, then call `✱9·13` and Q300's
`star_9_21 φ φ` explicitly. It cannot be `rfl` or retain an extra hypothesis.
`star_9_25` must call its immediately preceding local `star_9_23` instance at
`Apparent.ofElementary p ∨ₐ φ` and use only the certified `✱9·04`
normalization. No target is a primitive, and no theorem outside `Id`,
`✱9·13`, Q300 `✱9·21`, and `✱9·04` is licensed.
