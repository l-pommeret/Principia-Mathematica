import Principia.Syntax.Ramified
import Principia.FirstEdition.Volume1.Star95Source

namespace PM.RamifiedSyntax

/-!
# Derived propositions of PM I, ✱95

The relation arithmetic of ✱95 requires the ancestral, converse, relative
product, and restriction constructions as first-class relation arguments.
`star_21_01` has deliberately contextual scope and no pure derived theorem
currently eliminates that scope to such a term.  Host-language relation
operations would violate the object-judgement contract; no `⊢ᵣ` theorem is
therefore declared.
-/

end PM.RamifiedSyntax
