import Principia.Deduction.Star63Derived
import Principia.FirstEdition.Volume1.Star84Source

namespace PM.RamifiedSyntax

/-!
# Derived propositions of PM I, ✱84

The fifty-three asserted propositions requested from ✱84 concern mutually
exclusive classes and use class unions, intersections, class sums, selections,
and similarity.  The ramified calculus has contextual abstraction nodes but
no object-level definitions and conversion derivations for these operations;
the cited ✱51–✱53 and ✱81–✱83 results are likewise unavailable as `⊢ᵣ`
theorems.  Host-language set kernels are not admissible evidence.

No theorem is declared rather than certifying a surrogate formula.
-/

end PM.RamifiedSyntax
