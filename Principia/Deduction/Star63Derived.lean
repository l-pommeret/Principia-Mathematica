import Principia.Syntax.Ramified
import Principia.FirstEdition.Volume1.Star63Source

namespace PM.RamifiedSyntax

/-!
# Derived propositions of PM I, ✱63

The generic ramified sorts can type the orders occurring in ✱63, but its
relative-type operations have not yet been defined as reducible object-syntax
constructions.  The current `Star63TypeKernel2` operations are host-language
functions, so importing their equalities would fail the object-judgement
criterion.  No exact `⊢ᵣ` theorem is therefore declared here.
-/

end PM.RamifiedSyntax
