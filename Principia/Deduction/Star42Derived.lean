import Principia.Syntax.Ramified
import Principia.FirstEdition.Volume1.Star42Source

namespace PM.RamifiedSyntax

/-!
# Derived propositions of PM I, ✱42

All nine catalogue items are asserted, but none can yet be delivered as an
exact pure `⊢ᵣ` theorem.  The opening equalities ✱42·1 and ✱42·11 iterate the
class sum and product of ✱40; ✱42·12 and ✱42·13 iterate the relation sum and
product of ✱41.  Those operations are not standalone `Term`s in the present
ramified syntax because their defining class/relation abstractions remain
contextual `Formula.incompleteScope` nodes with no derivational elimination
rule.  The five later field and image identities depend on the same
operations and additionally on class-valued domain, converse-domain, and
field constructions.

Host equality and the extensional `Architecture` implementations are not
claims of `Derivation` and cannot certify these printed equations.  Hence the
honest result is zero of nine `⊢ᵣ` theorems.  This chapter becomes reachable
only after pure contextual-abstraction conversion is derived for ✱20/✱21 and
the resulting ✱40/✱41 operations are exposed inside the object calculus.
-/

end PM.RamifiedSyntax
