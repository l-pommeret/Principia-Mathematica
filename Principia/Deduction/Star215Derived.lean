import Principia.FirstEdition.Volume3.Star215Source
import Principia.Syntax.Ramified

namespace PM.RamifiedSyntax

/-!
# PM III, ✱215·01 — documentary hold

The frozen `PM-VERBATIM` block prints
`strʻP=α̂(α⊂CʻP.P̆ʻα∩Pʻα⊂α)` without `Df`, while `verbatimStatements` in the
same source appends `Df`.  No `star_215_01` declaration is introduced here:
deciding whether the locus is a definition would alter the documentary record
and belongs to the maintainer.
-/

end PM.RamifiedSyntax
