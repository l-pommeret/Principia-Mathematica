import Principia.Syntax.Ramified
import Principia.FirstEdition.Volume1.Star62Source

namespace PM.RamifiedSyntax

/-!
# Derived propositions of PM I, ✱62

`membership` supplies the object-language application shape needed for `ε`,
but the definitions and preceding class/relation results used in ✱62 have no
ramified definition-conversion or `⊢ᵣ` theorems yet.  The extensional
`Star62Kernel` propositions are Lean `Prop` statements and therefore cannot
serve as evidence for `Derivation`.  No theorem is declared until that missing
object-calculus layer exists.
-/

end PM.RamifiedSyntax
