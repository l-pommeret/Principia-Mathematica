import Principia.Syntax.Ramified
import Principia.FirstEdition.Volume1.Star97Source

namespace PM.RamifiedSyntax

/-!
# Derived propositions of PM I, ✱97

The final number of Volume I combines relation fields, identity, converse,
ancestral relations, images, and restrictions.  Its printed assertions still
require the missing pure elimination of contextual relation abstraction into
the relation-valued arguments of those operations.  Replacing them by Lean
relations would not derive PM's AST.  Thus no exact `⊢ᵣ` theorem is
declared pending that earlier derived conversion result.
-/

end PM.RamifiedSyntax
