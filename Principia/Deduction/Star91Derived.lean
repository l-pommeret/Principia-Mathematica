import Principia.Syntax.Ramified
import Principia.FirstEdition.Volume1.Star91Source

namespace PM.RamifiedSyntax

/-!
# Derived propositions of PM I, ✱91

The definitions and assertions of ✱91 apply converse, relative product, and
restriction to the ancestral introduced at ✱90·01.  Since that contextual
relation abstraction cannot yet be eliminated to a relation-valued object
term, none of these printed formulas has an exact `Claim.assertion` AST in the
current calculus.  No host relation or opaque propositional atom is substituted
for it, and therefore no `⊢ᵣ` theorem is declared.
-/

end PM.RamifiedSyntax
