# Rejected kernel prototypes

- `Star21PMKernel.lean`: rejected because `Support` constructors replaced real
  earlier derivations with fresh primitive assumptions.
- `Star23OpeningDerivation.lean`: rejected because printed `Df` definitions
  were incorrectly represented as constructors of a proposition-valued
  derivation relation.

These files are retained for diagnosis only and are not compiled or indexed.
